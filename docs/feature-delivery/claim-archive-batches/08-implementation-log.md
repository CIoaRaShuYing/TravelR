# 报销归档批次：实施日志

## 2026-09-09

- 当前里程碑：M1 领域与数据库。
- 前置提交：`083586c`，修复已确认餐补未进入月度报销汇总。
- 已完成：需求、业务边界、验收标准和技术设计定稿。
- subagent：未启用；用户未要求，主 Agent 执行等价分析与实施。
- 下一步：新增领域模型、EF 映射、归档资格规则和非破坏性迁移。

### M1 完成

- 新增 `ClaimArchiveBatch`、父报销可空外键、唯一名称/日期约束和查询索引。
- 生成非破坏性迁移 `AddClaimArchiveBatches`；历史报销保持未归档。
- 新增归档资格规则和 9 个定向测试，全部通过。
- 当前里程碑：M2 服务与接口。

### M2-M4 完成

- 新增归档预览、原子创建、列表、详情、并发令牌改名及对应审计接口；归档范围按中国时区 `SubmittedAt` 闭区间覆盖所有项目。
- 报销、餐补和费用明细查询统一支持 `archiveState` / `archiveBatchId`，并支持 `archiveBatch` 分组；审批与发放工作队列强制排除归档成员。
- 批次 ZIP 直接按 `ArchiveBatchId` 固定成员加载，不重新按日期检索；沿用既有四工作表和凭证整体失败语义。
- 所有报销工作流写操作统一增加 `CLAIM_ARCHIVED` 保护，申请人端同时隐藏归档记录的编辑与删除入口。

### M5 完成

- 报销管理新增“归档批次”工作台：日期预览、阻塞原因、创建并冻结、批次列表、改名和固定成员重导。
- 报销管理、报销看板、餐补看板新增归档状态/具体批次筛选与按归档划分；列表和详情展示批次名称。
- 前端 `vue-tsc` 与 Vite 生产构建通过；桌面端完整工作台及 390×844 移动端卡片布局均通过浏览器视觉验收。

### M6 隔离环境验收

- 在隔离 PostgreSQL 16 临时容器中从初始迁移顺序执行至 `20260909015950_AddClaimArchiveBatches`，全部成功；EF 模型漂移检查无待生成变更。
- 真实 HTTP 构造一笔差旅报销，完成报销审批/发放和餐补审批/发放后预览并归档；预览为报销 1 笔、餐补 1 笔、阻塞 0 笔。
- 归档后在相同提交日新增并结案另一笔报销；旧批次 ZIP 的工作簿仍只含原报销，且含 2 份原凭证，证明重导未按日期重新取数。
- 具体批次筛选结果：报销 1、餐补 1、费用明细 2；按归档分组报销 1。归档后新增版本返回 `CLAIM_ARCHIVED`。
- `ClaimArchiveBatchCreated`、`ClaimArchiveBatchRenamed`、`ClaimArchiveBatchExported` 审计各 1 条；数据库批次成员数保持 1。
- 普通申请人访问 `GET /api/admin/claim-archive-batches` 返回 403；管理员界面可正常加载固定批次、改名和重新导出入口。
- 浏览器验收发现批次列表投影后排序无法被 EF 翻译；已改为实体排序后再投影，并在真实 PostgreSQL 页面复验通过。
- 完整后端测试 70/70 通过；后端编译 0 警告/0 错误；前端生产构建通过（仅保留既有大 chunk 提示）。
- 验收进程、3 个临时附件和 PostgreSQL 临时容器已清理；未修改既有持久数据库。
- CAB-01 至 CAB-06 全部完成。
