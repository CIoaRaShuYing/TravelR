# 业务边界

## 角色

- 操作者必须是有效管理员。
- 升级对象必须是启用的正式用户；降级对象不能是当前操作者或超级管理员。
- 降级只移除 `Administrator`，保留 `Applicant` 与历史数据。

## 密码

- 个人改密必须验证原密码，新密码沿用至少 8 位且包含数字的 Identity 策略。
- 管理员重置不要求原密码，但只允许重置其他正式用户。
- 不记录任何密码、哈希或重置令牌。
