# 验收标准

- 普通管理员可升级启用申请人；升级后角色为 `Applicant` + `Administrator`。
- 普通管理员可取消其他非超级管理员角色；当前管理员不能自我取消。
- 超级管理员取消操作返回 `409`，页面显示不可操作状态。
- 角色变化后旧 JWT 返回 `401`，重新登录后权限与角色正确。
- 原密码错误、新密码不符合策略或两次新密码不一致时改密失败。
- 个人改密和管理员重置后旧密码不可登录，新密码可登录，旧 JWT 失效。
- 管理员不能通过重置接口修改自己密码。
- 桌面端和移动端均可操作，审计不包含密码敏感信息。
